# ============ REQUIRED ============
PORTKEY_API_KEY="mT3**************"
PORTKEY_CONFIG_ID="pc-resq-a-43e1f8"

# ============ AGENT MODE ============
# "prod" = Multi-agent LangGraph workflow (Supervisor → Triage → etc.)
# "test" = Single large model call (faster, for development)
AGENT_MODE="prod"

# ============ PORTKEY TIER CONFIGS (Optional) ============
# For latency optimization, create separate configs per model tier.
# If not set, all agents use PORTKEY_CONFIG_ID.
PORTKEY_CONFIG_FAST="pc-resq-fast-xxxxx"     # 8B models (Supervisor, Geo, Protocol, Vision)
PORTKEY_CONFIG_MEDIUM="pc-resq-medium-xxxxx" # 32B models (Triage)
PORTKEY_CONFIG_HEAVY="pc-resq-heavy-xxxxx"   # 70B models (Reflector)

# Optional: For direct fallback if Portkey is down
GROQ_API_KEY="gsk_*************************"

# ============ PORTKEY GUARDRAILS (OPTIONAL) ============
# Create guardrails in Portkey Dashboard -> Guardrails
# PORTKEY_GUARDRAILS_ID="gr-xxxxx"

# ============ QDRANT CLOUD ============
# Get from: https://cloud.qdrant.io → Your Cluster → API Keys
QDRANT_URL="https://your-cluster-id.region.gcp.cloud.qdrant.io:6333"
QDRANT_API_KEY="your-qdrant-api-key-here"

# ============ LOCAL SERVICES (Docker) ============
REDIS_URL=redis://redis:6379/0
POSTGRES_URL=postgresql://resq:resq@postgres:5432/resq

# ============ APP CONFIG ============
APP_ENV=development
DEBUG=true

# ============ FUTURE (WhatsApp - Skip for MVP) ============
# WHATSAPP_ACCESS_TOKEN=
# WHATSAPP_VERIFY_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
